# README Maintenance

After any change to the project, including source files, build system, docs, or configuration, update `README.md` to reflect the current state.

Review these sections:

- Structure: add, remove, or rename files as needed
- Status: check off completed features and add new planned items
- Boot sequence, Kernel, Dependencies, Build and Run: update any section affected by the change
